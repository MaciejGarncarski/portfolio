---
import { Image } from 'astro:assets';

export type Props = {
  image: ImageMetadata;
  title: string;
};

const { image, title } = Astro.props;
---

<Image
  src={image}
  alt={title}
  class="min-h-[10rem] w-auto border-2 border-primary lg:min-h-[22rem] lg:max-w-3xl lg:border-4 rounded"
/>
