---
import { Icon } from 'astro-icon/components';
import { technologies } from 'src/data/Technologies';
---

<ul
  class="mb-4 grid max-w-sm grid-cols-3 gap-6 md:gap-10 lg:mb-10 lg:max-w-md lg:gap-10">
  {
    technologies.map(({ name, fileName, link }) => {
      return (
        <li class="justify-self-start">
          <a
            class="flex flex-col items-center justify-center gap-2 border-4 bg-slate-100 rounded border-primary p-2 ring-primary hover:ring-2 focus:outline-none focus:ring-4 focus:ring-offset-4"
            href={link}
            target="_blank"
            rel="noopener noreferrer">
            <Icon name={`Logos${fileName}`} class="h-14 w-14 lg:h-20 lg:w-20" />
            <span class="visually-hidden">{name}</span>
          </a>
        </li>
      );
    })
  }
</ul>
